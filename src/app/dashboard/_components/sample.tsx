import React from 'react'

const SampleCom = ({ leftCred, radDeg }: { leftCred: number, radDeg: number }) => {
    console.log(leftCred, radDeg)
    return (
        <div>SampleCom</div>
    )
}

export default SampleCom